# 新增界面元素先对齐现有写法

**结论**：加按钮、链接、表单等界面元素前，先读项目里同款组件的实际代码，严格照抄属性、class、样式，不要凭空加装饰。

## 为什么

某次加下载按钮，因未对齐同款写法返工 3 轮。用户偏好：宁可多花一步先对齐，不要反复改。

## 怎么做

1. 找到项目里功能最相近的现成组件
2. 读它的完整代码：属性、事件、class、scoped 样式
3. 照抄结构，只改必要部分
4. 不自己加装饰（多余箭头、`target`、动画）

## 反例

凭印象写一个 `<a>` 当按钮，而项目里实际用的是封装好的组件，导致样式和行为都不一致。
