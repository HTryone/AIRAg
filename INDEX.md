# 索引

按领域分类。**分类规则、拆分粒度、写法模板见 `README.md`，加条目前先读。**

| 类型 | 含义 | 有新内容时 |
|---|---|---|
| `单条` | 一条独立经验，一个结论 | 相关就扩充这篇，不要新建 |
| `模块` | 一个工具 / 一条流程的完整文档 | **优先往里加章节**，不要另起一篇 |

条目总数：**32**（单条 24 + 模块 8）
最后核对：2026-08-29

加条目后必须同步改三处：**本行数字、对应分类表、底部体检表**。改了条目不动索引等于没写。

---

## 01-协作红线

| 类型 | 条目 | 一句话 |
|---|---|---|
| 单条 | [先给方案再动手](01-协作红线/先给方案再动手.md) | 非琐碎任务先给取舍方案，确认后再执行 |
| 单条 | [撤销改动必须用户授权](01-协作红线/撤销改动必须用户授权.md) | 删除、合并、拆分、移动文件都要先获批 |
| 单条 | [只说有证据的话](01-协作红线/只说有证据的话.md) | 禁用"可能/怀疑/大概率" |
| 单条 | [不擅自修改工作代码](01-协作红线/不擅自修改工作代码.md) | 没有运行时错误证据就不改 |
| 单条 | [新增界面元素先对齐现有写法](01-协作红线/新增界面元素先对齐现有写法.md) | 先读同款组件再照抄风格 |
| 单条 | [改动范围必须和授权一致](01-协作红线/改动范围必须和授权一致.md) | 授权改 A 就只动 A，改对了也不能改多 |

## 02-命令行与脚本

| 类型 | 条目 | 一句话 |
|---|---|---|
| 单条 | [禁止在文档里引入 shell 变量](02-命令行与脚本/禁止在文档里引入 shell 变量.md) | `$VAR` 换个窗口就失效 |
| 单条 | [Git Bash 路径写法](02-命令行与脚本/Git Bash 路径写法.md) | `/d/...` 传给 Windows 程序会变成 `d:\d\...` |
| 单条 | [pythonw 与 python 的区别](02-命令行与脚本/pythonw 与 python 的区别.md) | 后台用 pythonw，调试用 python |
| 单条 | [bat 文件用 ASCII 内容](02-命令行与脚本/bat 文件用 ASCII 内容.md) | 中文内容会乱码 |
| 单条 | [PowerShell 与 Bash 不能互调](02-命令行与脚本/PowerShell 与 Bash 不能互调.md) | 各有各的工具，硬调会被安全策略拦截 |
| 模块 | [Tauri v2 构建打包](02-命令行与脚本/Tauri%20v2%20构建打包.md) | 环境 → 从零做 → 一键构建 → 排查 → 避坑，产出 Windows NSIS + Android APK |
| 模块 | [重复文件清理](02-命令行与脚本/重复文件清理.md) | 分级筛选（大小 → 部分哈希 → 全哈希）→ 干跑 → 处理策略 → py/go 取舍 |

## 03-Windows 系统

| 类型 | 条目 | 一句话 |
|---|---|---|
| 模块 | [Windows 定时任务](03-Windows 系统/Windows 定时任务.md) | 在哪 → 从零新建 → 一键建 → 日常管理 → 排查 → 避坑 |
| 单条 | [Windows 中文乱码与编码](03-Windows 系统/Windows%20中文乱码与编码.md) | 读的编码和存的编码对不上，三个环节逐个查 |

## 04-逆向与抓包

| 类型 | 条目 | 一句话 |
|---|---|---|
| 模块 | [Electron 客户端逆向](04-逆向与抓包/Electron 客户端逆向.md) | 解包流程：找 asar → 提取 → 找凭据 → 试接口 → 判定能否脚本化 |

## 05-Git 与版本控制

| 类型 | 条目 | 一句话 |
|---|---|---|
| 单条 | [回退用 reset --hard 不用 revert](05-Git 与版本控制/回退用 reset --hard 不用 revert.md) | revert 会留一个多余的提交 |
| 单条 | [push 必须用户明确授权](05-Git 与版本控制/push 必须用户明确授权.md) | 「并入 main」不等于授权推送 |
| 单条 | [删仓库文件前先查跟踪状态](05-Git 与版本控制/删仓库文件前先查跟踪状态.md) | 别在错误目录 ls 还吞掉报错 |

## 06-文档与表达

| 类型 | 条目 | 一句话 |
|---|---|---|
| 单条 | [文档必须对读者可用](06-文档与表达/文档必须对读者可用.md) | 精简不删实操、文件名要核对、命令能直接复制 |
| 单条 | [说明文档用纯中文](06-文档与表达/说明文档用纯中文.md) | 不堆代码常量名 |

## 07-排错方法论

| 类型 | 条目 | 一句话 |
|---|---|---|
| 单条 | [改之前先拿证据](07-排错方法论/改之前先拿证据.md) | 取证优先级、一次改一处、两三版无效就停 |
| 单条 | [「之前能跑现在不行」怎么查](07-排错方法论/之前能跑现在不行怎么查.md) | 先 diff 中间改了什么 |
| 模块 | [内存持续增长怎么查](07-排错方法论/内存持续增长怎么查.md) | 先分清泄漏还是峰值 → 分阶段打点 → 四个常见来源 → 改完复测 |

## 08-网络与接口

| 类型 | 条目 | 一句话 |
|---|---|---|
| 单条 | [多端点容错](08-网络与接口/多端点容错.md) | 依次尝试取第一个成功 |
| 单条 | [写操作接口要做幂等](08-网络与接口/写操作接口要做幂等.md) | 先查状态再执行 |
| 单条 | [接口探测要有止损线](08-网络与接口/接口探测要有止损线.md) | 两三组合失败就判定不可行 |
| 模块 | [大文件传输调优](08-网络与接口/大文件传输调优.md) | 调参顺序（分片→并发→窗口→ack）、看振荡不看峰值、止损线 |

## 09-服务器运维

| 类型 | 条目 | 一句话 |
|---|---|---|
| 模块 | [SSH 服务器连接与管理](09-服务器运维/SSH%20服务器连接与管理.md) | 怎么连 → 密钥登录 → fail2ban 加固 → 日常管理 → 连不上怎么查 |
| 模块 | [证书申请与自动续期](09-服务器运维/证书申请与自动续期.md) | acme.sh：HTTP 验证 / DNS 验证 → 装到 Web 服务器 → 自动续期 → 排查 |
| 单条 | [出站 25 端口被封怎么办](09-服务器运维/出站%2025%20端口被封怎么办.md) | 云厂商默认封出站 25，改走 587 中继，别指望解封 |
| 单条 | [小内存服务器优化](09-服务器运维/小内存服务器优化.md) | 用 zram 不用磁盘 swap，算法选 zstd，swappiness 调到 100 |

---

## 目录体检

| 目录 | 条目数 | 状态 |
|---|---|---|
| 01-协作红线 | 6 | 正常 |
| 02-命令行与脚本 | 7 | 正常 |
| 03-Windows 系统 | 2 | 正常，还可沉淀：环境变量、注册表、服务管理 |
| 04-逆向与抓包 | 1 | 偏少，还可沉淀：抓包代理、protobuf、Web 端接口逆向 |
| 05-Git 与版本控制 | 3 | 正常 |
| 06-文档与表达 | 2 | 正常 |
| 07-排错方法论 | 3 | 正常 |
| 08-网络与接口 | 4 | 正常 |
| 09-服务器运维 | 4 | 2026-08-29 新建，还可沉淀：Docker 容器管理、反向代理、备份恢复 |

**超过 8 条**就要检查有没有能合并的，或者里面藏着该独立成篇的子主题。
