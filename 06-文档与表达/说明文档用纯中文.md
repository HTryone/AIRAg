# 说明文档用纯中文

**结论**：说明类、参数类文档用纯中文书写，不堆代码常量名和英文术语。

## 为什么

用户看不懂英文常量名。满屏 `expiresAt`、`StartWhenAvailable` 对他没有信息量。

## 怎么做

- 用中文描述作用，常量名只在"要照着填"的时候出现（比如命令里必须写 `-At 09:00`）
- 每个参数都要写**作用说明**，不能只列名字和数值
- 二进制 / 帧结构用中括号分层表示：

  ```
  [12B 帧头][16B IV][密文 = 你的文件][32B HMAC]
  ```

- 讲解用大白话 + 图表，不用术语堆砌
